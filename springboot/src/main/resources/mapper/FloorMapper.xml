<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.FloorMapper">
    <select id="findPage" resultType="com.example.demo.entity.Floor">
        select floor.*,sys_dormitory.name as adminName from floor
        left join sys_dormitory
        on floor.administrators = sys_dormitory.id
        <where>
            <if test="search != null and search != ''">
                and floor.floor_name like concat('%', #{search},'%')
            </if>
        </where>
    </select>

</mapper>
